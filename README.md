# Fullstack Shopping List App for Docker

Frontend - React

Backend - Flask, SQLite  

## How to Run

Open Docker. Navigate into root directory via terminal. Run `docker-compose up --build`. App will be hosted on `localhost:80`.